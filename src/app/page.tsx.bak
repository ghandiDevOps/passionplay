import Image from "next/image";
import Link from "next/link";

const EXAMPLE_SESSIONS = [
  { title: "Défense au sol en MMA", meta: "Paris 11e · Sam 10h", price: "15 €", spots: "4 places" },
  { title: "Tir à 3 points en basket", meta: "Lyon 3e · Dim 14h", price: "18 €", spots: "7 places" },
  { title: "Contrôle orienté en foot", meta: "Paris 20e · Jeu 19h", price: "13 €", spots: "2 places" },
  { title: "Prise de parole en public", meta: "Paris 9e · Sam 09h", price: "20 €", spots: "10 places" },
];

export default function HomePage() {
  return (
    <main className="min-h-screen bg-white text-slate-950">
      <div className="page-container">
        <header className="flex flex-wrap items-center justify-between gap-4 border-b border-slate-200 py-6">
          <Link href="/" className="inline-flex items-center gap-2">
            <Image
              src="/PassionPlay_logoavectext_svg.svg"
              alt="PassionPlay"
              width={220}
              height={42}
              priority
            />
          </Link>
          <nav className="flex flex-wrap items-center gap-3 text-sm uppercase tracking-[0.14em] text-slate-700">
            <Link href="/explore" className="font-semibold hover:text-slate-900">
              Sessions
            </Link>
            <Link href="/sign-in" className="font-semibold hover:text-slate-900">
              Connexion
            </Link>
          </nav>
        </header>

        <section className="grid gap-10 py-12 lg:grid-cols-[1.2fr_0.8fr] lg:items-end">
          <div className="space-y-8">
            <div className="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-slate-50 px-4 py-2 text-xs font-semibold uppercase tracking-[0.24em] text-slate-700">
              <span className="h-2 w-2 rounded-full bg-passion-500" />
              Sessions live · sport · passion
            </div>

            <div className="space-y-6">
              <h1 className="text-4xl font-black tracking-[-0.04em] leading-tight sm:text-5xl md:text-6xl">
                Vis ta passion.
                <br />
                <span className="text-passion-500">Pas que la rêver.</span>
              </h1>
              <p className="max-w-2xl text-base leading-8 text-slate-400 sm:text-lg">
                Des sessions intenses, en petit groupe. Des coachs experts qui transforment ton temps en progrès concret.
              </p>
            </div>

            <div className="grid gap-4 sm:grid-cols-2">
              <Link href="/explore" className="btn-primary">
                Trouver une session →
              </Link>
              <Link href="/sign-up" className="btn-secondary">
                Je suis coach, je crée ma session
              </Link>
            </div>

            <div className="grid gap-3 sm:grid-cols-3">
              <FeatureCard label="Prix" value="13€–20€" />
              <FeatureCard label="Durée" value="1h" />
              <FeatureCard label="Groupe" value="Max 10" />
            </div>
          </div>

          <aside className="rounded-[2rem] border border-slate-200 bg-slate-50 p-6 shadow-sm">
            <div className="rounded-[1.75rem] bg-passion-500/10 p-6 text-slate-950 shadow-sm">
              <p className="text-xs uppercase tracking-[0.24em] text-passion-700">Coach spotlight</p>
              <h2 className="mt-4 text-3xl font-black">Karim · MMA</h2>
              <p className="mt-3 text-sm leading-7 text-slate-700">
                12 ans d’expérience. Groupe réduit. Une session pensée pour te faire progresser dès le premier cercle.
              </p>
            </div>

            <div className="mt-6 grid gap-4 sm:grid-cols-2">
              <StatCard label="Note" value="4.9/5" />
              <StatCard label="Places restantes" value="4" />
            </div>
          </aside>
        </section>

        <section className="grid gap-6 py-12 lg:grid-cols-2">
          <div className="rounded-[2rem] border border-slate-200 bg-slate-50 p-8">
            <p className="text-xs uppercase tracking-[0.24em] text-passion-500">Découverte</p>
            <h2 className="mt-3 text-2xl font-black">Première fois</h2>
            <p className="mt-4 text-sm leading-7 text-slate-700">
              Tu n’as jamais essayé. Tu veux ressentir pour la première fois. Aucun prérequis.
            </p>
          </div>
          <div className="rounded-[2rem] border border-slate-200 bg-slate-50 p-8">
            <p className="text-xs uppercase tracking-[0.24em] text-sky-500">Progression</p>
            <h2 className="mt-3 text-2xl font-black">Débloquer un point</h2>
            <p className="mt-4 text-sm leading-7 text-slate-700">
              Tu pratiques déjà. Tu veux améliorer un geste précis avec un coach qui sait où t'amener.
            </p>
          </div>
        </section>

        <section className="rounded-[2rem] border border-slate-800 bg-slate-900/95 p-8 shadow-xl shadow-passion-900/20">
          <div className="flex flex-col gap-6 md:flex-row md:items-end md:justify-between">
            <div>
              <p className="text-xs uppercase tracking-[0.24em] text-slate-500">Des moments qui existent vraiment</p>
              <h2 className="mt-3 text-3xl font-black">Sessions en direct</h2>
            </div>
            <Link href="/explore" className="text-sm font-semibold uppercase tracking-[0.14em] text-passion-500 hover:text-passion-400">
              Voir toutes les sessions →
            </Link>
          </div>

          <div className="mt-8 grid gap-4 sm:grid-cols-2">
            {EXAMPLE_SESSIONS.map((session) => (
              <div key={session.title} className="rounded-[1.75rem] border border-slate-800 bg-slate-950/90 p-6 shadow-sm transition hover:-translate-y-1 hover:border-passion-500">
                <span className="inline-flex rounded-full bg-slate-800 px-3 py-1 text-xs uppercase tracking-[0.24em] text-slate-400 mb-4">
                  {session.spots} restantes
                </span>
                <h3 className="text-xl font-black text-white">{session.title}</h3>
                <p className="mt-3 text-sm leading-7 text-slate-400">{session.meta}</p>
                <div className="mt-6 flex items-center justify-between gap-4">
                  <p className="text-lg font-bold text-passion-500">{session.price}</p>
                  <Link href="/explore" className="text-sm font-semibold uppercase tracking-[0.14em] text-passion-500 hover:text-passion-400">
                    Réserver
                  </Link>
                </div>
              </div>
            ))}
          </div>
        </section>

        <footer className="border-t border-slate-800 py-12 text-center text-sm text-slate-500">
          PassionPlay © 2026
        </footer>
      </div>
    </main>
  );
}

function FeatureCard({ label, value }: { label: string; value: string }) {
  return (
    <div className="rounded-3xl border border-slate-800 bg-slate-900/95 p-5 text-center">
      <p className="text-xs uppercase tracking-[0.24em] text-slate-500">{label}</p>
      <p className="mt-3 text-2xl font-black text-white">{value}</p>
    </div>
  );
}

function StatCard({ label, value }: { label: string; value: string }) {
  return (
    <div className="rounded-3xl border border-slate-800 bg-slate-900/95 p-5 text-center">
      <p className="text-xs uppercase tracking-[0.24em] text-slate-500">{label}</p>
      <p className="mt-3 text-2xl font-black text-white">{value}</p>
    </div>
  );
}
